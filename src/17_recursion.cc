#include "17_recursion.h"
#include <vector>

// Calculate factorial recursively
unsigned long long factorial_recursion(int n) {
  // Your code here
  return 0; // Replace with correct implementation
}

// Calculate Fibonacci number recursively
int fibonacci(int n) {
  // Your code here
  return 0; // Replace with correct implementation
}

// Calculate sum of digits recursively
int sum_of_digits(int n) {
  // Your code here
  return 0; // Replace with correct implementation
}

// Check if a string is a palindrome recursively
bool is_palindrome(const std::string &str, int start, int end) {
  // Your code here
  return false; // Replace with correct implementation
}

// Wrapper function for palindrome check
bool is_palindrome(const std::string &str) {
  return is_palindrome(str, 0, str.length() - 1);
}

// Calculate power recursively (x^n)
double power(double x, int n) {
  // Your code here - handle negative exponents
  return 0; // Replace with correct implementation
}
